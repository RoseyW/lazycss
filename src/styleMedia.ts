import cssList from "./cssList";

export interface styleMedia {

}

const useMedia = function (mediaType: string, mediaFeature: string, featureValue: string|number, styleList: cssList) {

}

export default useMedia;
